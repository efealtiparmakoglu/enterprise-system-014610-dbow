apiVersion: v1
kind: Service
metadata:
  name: enterprise-system-014610-dbow-service
spec:
  selector:
    app: enterprise-system-014610-dbow
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
